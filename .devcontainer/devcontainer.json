{
  "name": "Drupal 11 with DDEV",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "forwardPorts": [80, 443],
  "postCreateCommand": "ddev start && ddev composer create drupal/recommended-project my-drupal-site && ddev exec drush site-install --account-name=admin --account-pass=admin -y",
  "customizations": {
    "vscode": {
      "extensions": [
        "davidanson.vscode-markdownlint",
        "esbenp.prettier-vscode",
        "bmewburn.vscode-intelephense-client",
        "xdebug.php-debug"
      ]
    }
  },
  "remoteUser": "vscode"
}

